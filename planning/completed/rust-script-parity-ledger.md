# Rust Script Parity Ledger

Generated: 2026-03-28 19:39

## Status

- LastUpdated: 2026-04-05 15:00
- Objective: define the parity evidence model and the current closeout status that every PowerShell migration domain must satisfy before wrapper cutover.
- Source Plan: `planning/completed/plan-repository-unification-and-rust-migration.md`
- Supporting Matrix: `planning/completed/rust-script-transcription-ownership-matrix.md`
- Cutover Map: `planning/completed/rust-script-cutover-default-map.md`
- Archived From Branch: `feature/native-validation-policy`
- Remaining Open Backlog: none within the completed migration bundle; follow-up work is now limited to future domain-level consumer sweeps outside this closed retirement sequence.
- Live parity harness: `approval-approved-test`, the staged `run-test` closeout success path, `evaluate-agent-pipeline`, and `resume-agent-pipeline` are now covered by the native orchestrator harness in `crates/orchestrator/tests/execution/pipeline_parity`
- Parity cleanup note: tracked repository artifacts now restore through Git-backed recovery, and projected POML assets are restored explicitly after parity runs so the worktree returns to a clean baseline deterministically.
- Current closeout stance: the explicit cutover map now records which parity-proven domains are Rust-default now, intentionally wrapper-retained, or retained as legacy integration wrappers; the parity ledger still avoids promoting a domain to `cutover ready` by implication alone.
- Follow-up note: the former `validate-agent-orchestration`, `validate-release-governance`, `validate-release-provenance`, `validate-compatibility-lifecycle-policy`, `validate-dotnet-standards`, `validate-authoritative-source-policy`, `validate-instruction-architecture`, `validate-planning-structure`, `validate-readme-standards`, `validate-template-standards`, `validate-workspace-efficiency`, `validate-instruction-metadata`, `validate-agent-hooks`, `validate-shell-hooks`, `validate-runtime-script-tests`, `validate-instructions`, `test-routing-selection`, `validate-all`, `export-audit-report`, `export-enterprise-trends`, `doctor`, `healthcheck`, `sync-codex-mcp-config`, `render-vscode-mcp-template`, `self-heal`, `render-provider-surfaces`, `render-codex-compatibility-surfaces`, and `render-codex-orchestration-surfaces` leaves were retired locally in Phases 9 through 20f, so the remaining follow-up is domain-level consumer proof outside the validation folder rather than single-leaf wrapper cutover.
- Closeout board semantics:
  - `parity proven`: native Rust behavior and deterministic evidence exist; the cutover map then decides whether the domain is Rust-default now or wrapper retained intentionally
  - `cutover ready`: parity is proven and the domain is ready to become Rust-default
  - `retired locally`: parity was proven and the local compatibility wrapper was removed from this repository
  - `wrapper retained intentionally`: parity exists, but the legacy wrapper remains part of the approved operating model
  - `retained wrapper exception`: the remaining shell-owned behavior is explicitly retained by policy and is not treated as open migration debt
  - `evidence gap remains`: the owner boundary exists, but the recorded closeout evidence is still incomplete

## Evidence Policy

1. A migration slice is not parity-complete until the owning Rust crate has deterministic tests for the migrated behavior.
2. Wrapper cutover is not allowed with compile-only evidence; each migrated domain also needs at least one operator-path smoke check.
3. Validation-owned domains must preserve or improve current policy severity behavior before the PowerShell logic can be retired.
4. Test-owned domains must replace the PowerShell harness with Rust-native assertions in the owning crates or a root integration harness.
5. Docs and CHANGELOG updates are part of parity for user-visible or operator-visible command changes.

## Domain Ledger

| Legacy Scope | Count | Rust Owner | Required Evidence | Current Status | Closeout Notes |
| --- | ---: | --- | --- | --- | --- |
| `scripts/common/*.ps1` | 15 | `crates/core` | unit tests for path/catalog/runtime helpers plus fixture-based parity assertions | `parity proven` | Shared helper foundations are implemented and covered, and the cutover map now marks this domain Rust-default now. |
| `scripts/runtime/*.ps1` excluding hooks | 30 | `crates/commands/runtime + crates/cli` | command-contract tests, CLI smoke checks, wrapper delegation proof | `parity proven` | Wave 1 runtime surface is implemented in Rust, docs present PowerShell as a compatibility layer, and the cutover map now marks the remaining domain Rust-default now after the continuity/template leaves were retired locally in Phase 4, the diagnostics pair was retired locally in Phase 17, the two MCP command wrappers were retired locally in Phase 18, `self-heal` was retired locally in Phase 20c, `render-provider-surfaces` was retired locally in Phase 20d, `render-codex-compatibility-surfaces` was retired locally in Phase 20e, and `render-codex-orchestration-surfaces` was retired locally in Phase 20f while `render-mcp-runtime-artifacts.ps1` stays temporarily retained for the path-backed provider-surface catalog; Phase 20 Slice A then confirmed that the remaining projection/profile/sync/workspace leaves still have live authored consumers in catalogs, provider READMEs, `install.ps1`, parity tests, and validation fixtures, so parity remains proven but cutover is still blocked. |
| `scripts/runtime/hooks/common.ps1`, `scripts/runtime/hooks/session-start.ps1`, `scripts/runtime/hooks/subagent-start.ps1` | 3 | `crates/commands/runtime + crates/orchestrator` | hook contract tests, orchestration integration tests, local hook dispatch smoke | `retained wrapper exception` | These three startup wrappers remain the approved compatibility launch surfaces for the VS Code/Codex hook contract. |
| `scripts/runtime/hooks/pre-tool-use.ps1` | 1 | `crates/commands/runtime + crates/cli` | native hook tests, CLI runtime smoke checks, projected wrapper dispatch proof | `retired locally` | The local leaf was removed after `.github/hooks/scripts/pre-tool-use.ps1` and the provider-authored wrapper moved to `ntk runtime pre-tool-use`. |
| `scripts/runtime/update-local-context-index.ps1`, `scripts/runtime/query-local-context-index.ps1`, `scripts/runtime/export-planning-summary.ps1`, `scripts/runtime/apply-vscode-templates.ps1` | 4 | `crates/commands/runtime + crates/cli` | CLI contract tests, continuity/runtime parity tests, authored consumer cutover proof | `retired locally` | The four local continuity/template leaves were removed after runtime housekeeping, self-heal, authored docs, validation inventory, and parity tests moved to the native `ntk runtime` executable contract. |
| `scripts/runtime/doctor.ps1`, `scripts/runtime/healthcheck.ps1` | 2 | `crates/commands/runtime + crates/cli` | runtime crate tests, CLI command tests, native `doctor` smoke, native `healthcheck` smoke, and consumer repoint proof across runtime scripts and retained parity fixtures | `retired locally` | The local diagnostics wrappers were removed after `ntk runtime doctor` became the canonical drift-diagnosis entrypoint and all same-slice runtime consumers stopped requiring the deleted wrapper paths. |
| `scripts/runtime/sync-codex-mcp-config.ps1`, `scripts/runtime/render-vscode-mcp-template.ps1` | 2 | `crates/commands/runtime + crates/cli` | runtime crate tests, CLI command tests, native MCP render/apply smoke proof, bootstrap repoint proof, and retained runtime parity proof | `retired locally` | The two local MCP wrappers were removed after `ntk runtime` exposed the native MCP command surfaces, bootstrap and projected Codex compatibility wrappers stopped requiring the deleted paths, and the runtime parity scripts moved onto the native contract. |
| `scripts/runtime/self-heal.ps1` | 1 | `crates/commands/runtime + crates/cli` | runtime crate tests, CLI command tests, native `self-heal` smoke proof, and same-slice docs/policy/parity consumer repoint proof | `retired locally` | The local remediation wrapper was removed after `ntk runtime self-heal` became the canonical operator entrypoint and the remaining docs, policy inventory, and retained parity fixtures stopped requiring the deleted path. |
| `scripts/runtime/render-provider-surfaces.ps1` | 1 | `crates/commands/runtime + crates/cli` | runtime crate tests, CLI command tests, native `render-provider-surfaces` help and summary smoke proof, bootstrap repoint proof, runtime parity harness repoint proof, and orchestrator validation-baseline repoint proof | `retired locally` | The local provider-surface dispatcher wrapper was removed after `ntk runtime render-provider-surfaces` became the canonical operator entrypoint and bootstrap, authored docs, runtime parity fixtures, and orchestrator support stopped requiring the deleted path. |
| `scripts/runtime/render-codex-compatibility-surfaces.ps1` | 1 | `crates/commands/runtime + crates/cli` | runtime crate tests, CLI command tests, native `render-provider-surfaces --renderer-id codex-compatibility-surfaces` render smoke proof, shared provider-surface catalog helper native-dispatch proof, and runtime parity coverage repoint proof | `retired locally` | The local Codex compatibility renderer wrapper was removed after the provider-surface projection catalog adopted mixed native/script-backed dispatch, the shared helper learned to invoke the native runtime renderer contract, and the runtime parity suite stopped requiring the deleted path. |
| `scripts/runtime/render-codex-orchestration-surfaces.ps1` | 1 | `crates/commands/runtime + crates/cli` | runtime crate tests, CLI command tests, native `render-provider-surfaces --renderer-id codex-orchestration-surfaces` render smoke proof, runtime parity coverage repoint proof, and authored/projected Codex orchestration README repoint proof | `retired locally` | The local Codex orchestration renderer wrapper was removed after the provider-surface catalog entry moved onto the native runtime dispatcher, the runtime parity suite stopped requiring the deleted path, and both authored and projected Codex orchestration READMEs advertised the native renderer contract instead of the deleted wrapper. |
| `scripts/maintenance/generate-http-from-openapi.ps1` | 1 | `crates/commands/runtime` | external tool wrapper contract plus protected execution-path assertions | `retained wrapper exception` | The generator remains an explicit retained wrapper around the external OpenAPI reader toolchain. |
| `scripts/maintenance/clean-build-artifacts.ps1`, `scripts/maintenance/fix-region-spacing.ps1`, `scripts/maintenance/fix-version-ranges.ps1`, `scripts/maintenance/trim-trailing-blank-lines.ps1` | 4 | `crates/commands/runtime + crates/cli` | command tests plus filesystem fixture validation for mutation-heavy flows | `retired locally` | All four local maintenance leaves were removed after their live consumers moved to native runtime commands and the remaining operator entrypoints were narrowed to retained compatibility wrappers only where policy requires them. |
| `scripts/validation/*.ps1` | 0 | `crates/commands/validation + crates/commands/runtime + crates/cli` | policy tests, fixture validation, severity-preservation assertions, native CLI ownership, and operator-path smoke evidence for the reporting closeout | `retired locally` | The validation-folder script estate is fully retired locally after `validate-all` moved to `ntk validation all`, audit-report export moved to `ntk runtime healthcheck`, and enterprise-trends export moved to `ntk runtime export-enterprise-trends`. |
| `scripts/security/*.ps1` | 6 | `crates/commands/validation` | security gate tests, tool invocation contracts, failure-path assertions | `parity proven` | Native security policy coverage is recorded in the validation crate, and the cutover map now marks this domain Rust-default now. |
| `scripts/governance/*.ps1` | 2 | `crates/commands/validation` | governance contract tests plus safe no-op / denied-path assertions | `parity proven` | Governance checks are implemented natively through the validation surface, and the cutover map now marks this domain Rust-default now. |
| `scripts/doc/*.ps1` | 1 | `crates/commands/validation` | documentation validation tests against fixture docs | `parity proven` | `validate-xml-documentation` now exists natively in `crates/commands/validation/documentation`, is routed through `validate-all`, and the cutover map now marks this domain Rust-default now. |
| `scripts/deploy/*.ps1` | 1 | `crates/commands/validation` | deploy preflight tests and protected execution-path assertions | `parity proven` | `validate-deploy-preflight` now exists natively in `crates/commands/validation/deploy`, runs through `validate-all`, and the PowerShell deploy executor is intentionally retained as a compatibility wrapper. |
| `scripts/orchestration/**/*.ps1` | 10 | `crates/orchestrator` | staged execution tests, resume/replay assertions, dispatch integration checks | `parity proven` | Wave 3 control-plane parity is implemented and recorded, and the cutover map now marks this domain Rust-default now. |
| `scripts/git-hooks/*.ps1` | 3 | `crates/commands/runtime` | git hook install/check tests plus local hook bootstrap smoke | `parity proven` | Native git hook install and EOF hygiene coverage exists, but the hook surface remains a compatibility wrapper intentionally retained in the cutover map. |
| `scripts/tests/check-test-naming.ps1` | 1 | `crates/commands/validation` | validation-fixture coverage plus `validate-all` profile routing | `retired locally` | `validate-test-naming` remains native in `crates/commands/validation/operational_hygiene`, and the local PowerShell wrapper was removed after the validation surface contracts stopped locking the `.ps1` path. |
| `scripts/tests/refactor_tests_to_aaa.ps1` | 1 | `crates/commands/validation` | direct command tests plus validation-surface contract coverage | `retired locally` | `refactor_tests_to_aaa` remains native in `crates/commands/validation/operational_hygiene`, and the local PowerShell wrapper was removed after the validation surface contracts stopped locking the `.ps1` path. |
| `scripts/validation/validate-routing-coverage.ps1`, `scripts/validation/validate-architecture-boundaries.ps1`, `scripts/validation/validate-audit-ledger.ps1` | 3 | `crates/commands/validation + crates/cli` | native validation command tests, validation crate tests, and targeted `validate-all` profile proof | `retired locally` | The three low-fanout validation leaves were removed after `ntk validation` became the executable contract and `validate-all.ps1` plus policy/inventory surfaces stopped requiring the local wrapper paths. |
| `scripts/validation/validate-powershell-standards.ps1`, `scripts/validation/validate-shared-script-checksums.ps1`, `scripts/validation/validate-warning-baseline.ps1` | 3 | `crates/commands/validation + crates/cli` | native validation command tests, validation crate tests, and targeted `validate-all` phase-6 profile proof | `retired locally` | The three policy-heavy validation wrappers were removed after `ntk validation` exposed the executable contract and `validate-all.ps1`, validation inventory, and governance/release evidence stopped requiring the local wrapper paths. |
| `scripts/validation/validate-security-baseline.ps1`, `scripts/validation/validate-supply-chain.ps1` | 2 | `crates/commands/validation + crates/cli` | native validation command tests, validation crate tests, focused `validate-all` phase-7 profile proof, provider-surface re-render proof, and post-review regression coverage for multi-match scanning and missing license-evidence paths | `retired locally` | The two security-focused validation wrappers were removed after `ntk validation` exposed the executable contract and `validate-all.ps1`, validation inventory, governance/release evidence, and provider-authored skill examples stopped requiring the local wrapper paths; the removed wrapper evidence was replaced by native Rust implementation files in the required-evidence baselines. |
| `scripts/validation/validate-policy.ps1`, `scripts/validation/validate-agent-skill-alignment.ps1`, `scripts/validation/validate-agent-permissions.ps1` | 3 | `crates/commands/validation + crates/cli` | native validation command tests, validation crate tests, focused `validate-all` phase-8 profile proof, and `validate-stage.ps1` native dispatch proof | `retired locally` | The three local wrappers were removed after `ntk validation` exposed the executable contract, `validate-all.ps1` and `validate-stage.ps1` stopped requiring the wrapper paths, and policy/governance baselines replaced legacy `.ps1` evidence with native Rust files. |
| `scripts/validation/validate-agent-orchestration.ps1`, `scripts/validation/validate-release-governance.ps1`, `scripts/validation/validate-release-provenance.ps1` | 3 | `crates/commands/validation + crates/cli` | native validation command tests, validation crate tests, focused `validate-all` phase-9 profile proof, `validate-stage.ps1` native dispatch proof, `run-agent-pipeline.ps1` native preflight proof, and provider-surface re-render proof | `retired locally` | The three local wrappers were removed after `ntk validation` exposed the executable contract, the PowerShell consumer chain stopped requiring the wrapper paths, governance/policy baselines replaced legacy wrapper evidence with native Rust owners, and the projected `.codex` and `.claude` surfaces were re-rendered to advertise the native contract. |
| `scripts/validation/validate-compatibility-lifecycle-policy.ps1`, `scripts/validation/validate-dotnet-standards.ps1` | 2 | `crates/commands/validation + crates/cli` | native validation command tests, validation crate tests, focused `validate-all` phase-10 profile proof, `validate-instructions.ps1` inventory proof, policy evidence repoint, and compatibility runtime parity proof through the native `ntk validation` contract | `retired locally` | The two local wrappers were removed after `ntk validation` exposed the executable contract, `validate-all.ps1` stopped requiring the wrapper paths, direct dotnet wrapper evidence was replaced by the native Rust owner, and the compatibility runtime parity test moved onto the native validation contract. |
| `scripts/validation/validate-authoritative-source-policy.ps1`, `scripts/validation/validate-instruction-architecture.ps1` | 2 | `crates/commands/validation + crates/cli` | native validation command tests, validation crate tests, focused `validate-all` phase-11 profile proof, instruction inventory evidence repoint, governance baseline repoint, and runtime parity proof through the native `ntk validation` contract | `retired locally` | The two local wrappers were removed after `ntk validation` exposed the executable contract, `validate-all.ps1` stopped requiring the wrapper paths, instruction/governance baselines replaced legacy `.ps1` evidence with native Rust owners, and the runtime parity tests moved onto the native validation contract. |
| `scripts/validation/validate-planning-structure.ps1`, `scripts/validation/validate-readme-standards.ps1`, `scripts/validation/validate-template-standards.ps1`, `scripts/validation/validate-workspace-efficiency.ps1`, `scripts/validation/validate-instruction-metadata.ps1` | 5 | `crates/commands/validation + crates/cli` | native validation command tests, validation crate tests, focused `validate-all` phase-12 profile proof in warning-only and enforcing modes, `validate-instructions.ps1` inventory proof, `validate-stage.ps1` native dispatch proof, runtime parity proof through the native `ntk validation` contract, and provider-surface re-render proof | `retired locally` | The five local wrappers were removed after `ntk validation` exposed the executable contract, the PowerShell consumer chain stopped requiring the wrapper paths, the checklist/template guidance and governance baselines replaced legacy wrapper evidence with native Rust owners, and the runtime parity tests moved onto the native validation contract. |
| `scripts/validation/validate-agent-hooks.ps1`, `scripts/validation/validate-shell-hooks.ps1`, `scripts/validation/validate-runtime-script-tests.ps1` | 3 | `crates/commands/validation + crates/cli` | native validation command tests, validation crate tests, focused `validate-all` phase-13 profile proof in warning-only mode, runtime parity fixture repoint, release/security evidence repoint, and Codex compatibility-surface re-render proof | `retired locally` | The three local wrappers were removed after `ntk validation` exposed the executable contract, `validate-all.ps1` stopped requiring the wrapper paths, runtime parity fixtures and governance baselines replaced legacy `.ps1` evidence with native Rust owners, and the Codex compatibility surfaces were re-rendered to advertise the native contract. |
| `scripts/validation/validate-instructions.ps1`, `scripts/validation/test-routing-selection.ps1` | 2 | `crates/commands/validation + crates/cli` | native validation command tests, validation crate tests, focused `validate-all` phase-14 profile proof in warning-only mode, `validate-stage.ps1` native dispatch proof, authored checklist/runbook evidence repoint, and release-provenance support for the native `validate-all` contract | `retired locally` | The instruction coverage wrapper and redundant routing-selection leaf were removed after `ntk validation` exposed native `instructions` and `all` executable boundaries, the routing golden coverage moved directly into Rust instruction validation, the live consumer chain stopped requiring the wrapper paths, and governance/release evidence replaced legacy `.ps1` proof with native Rust owners or commands. |
| `scripts/validation/validate-all.ps1` | 1 | `crates/commands/validation + crates/cli` | native validation command tests, validation crate tests, warning-only native `validate-all` proof, runtime healthcheck native dispatch proof, and repository consumer repoint proof across runbooks/governance/projected skills | `retired locally` | The local orchestration wrapper was removed after `ntk validation all` became the canonical validation entrypoint, runtime/orchestration consumers stopped requiring the `.ps1` path, and governance/runbook evidence was repointed to the native contract. |
| `scripts/validation/export-audit-report.ps1`, `scripts/validation/export-enterprise-trends.ps1` | 2 | `crates/commands/runtime + crates/cli` | runtime crate tests, CLI contract tests, native `ntk runtime healthcheck` smoke proof, native `ntk runtime export-enterprise-trends` smoke proof, and governance/runbook evidence repoint to runtime diagnostics owners | `retired locally` | The final validation-folder reporting leaves were removed after the audit-report path moved to native runtime healthcheck and the enterprise trends exporter moved into runtime diagnostics without creating a runtime/validation crate cycle. |
| `scripts/tests/*.ps1` excluding `check-test-naming.ps1`, `refactor_tests_to_aaa.ps1`, and runtime subfolder | 2 | `crate test suites + root parity harness` | Rust-native replacements for coverage/test-shape automation | `retained wrapper exception` | The remaining two scripts, `apply-aaa-pattern` and `run-coverage`, are explicit retained wrapper exceptions in the cutover map rather than unresolved migration slices. |
| `scripts/tests/runtime/*.ps1` | 23 | `crate test suites + root parity harness` | root integration harness plus owning-crate assertions for each replaced runtime test | `parity proven` | The native parity harness covers `approval-approved`, staged `run-test` closeout, `evaluate-agent-pipeline`, and `resume-agent-pipeline`; the wrapper surface is retained intentionally as a compatibility launch surface, and parity cleanup is now stable. |

## Acceptance Gate

- `Ready for wrapper cutover`: Rust implementation exists, deterministic tests pass, operator smoke checks pass, docs are updated, and the domain row is updated from `implementation pending` or `planning locked` to `parity proven`.
- `Not ready for wrapper cutover`: any missing Rust test surface, missing smoke evidence, missing docs update, or unresolved policy regression.