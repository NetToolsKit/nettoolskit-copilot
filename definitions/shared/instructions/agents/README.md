# Agent Instruction Lane

`agents/` is the canonical source for agent-controller and orchestration guidance rather than repository invariants or platform implementation policy.

- `ntk-agents-super-agent.instructions.md`: mandatory intake-to-closeout controller lifecycle for change-bearing work

Keep repository-wide invariants under `core/` and keep technical implementation guidance under `process/`, `architecture/`, `operations/`, `data/`, `security/`, or `docs/`.