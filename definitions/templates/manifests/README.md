# Manifest Templates

> Canonical reusable manifest templates consumed by tooling and generation workflows.

---

Current canonical manifests:

- `control-plane-introspection.catalog.json` - machine-readable AI/runtime doctor schema kinds, entry points, and versioning rules
- `extension-governance.catalog.json` - extension classes, authored roots, discovery rules, and runtime loading boundaries
- `free-llm-provider-matrix.catalog.json` - free-provider family classification and quota/fallback hints
- `free-llm-provider-harness.catalog.json` - deterministic offline/live-opt-in provider harness contract
- `operational-memory-layering.catalog.json` - layered editorial model for entrypoint memory, topic memory, operational notes, and retrieval alignment
- `runtime-diagnostics.taxonomy.json` - canonical runtime health states, subsystem ownership, and degraded-state evidence rules

---